---
layout: 'search'
class : 'library'
pagination:
  data: collections.libraryTags
  size: 1
  alias: tag
permalink: /{{ class }}/search/tags/{{ tag }}/
title: Library tagged with {{ tag }}
---

<h1>Library for tag "{{ tag }}"</h1>

<ul class="wordlist">
  {% set libraryList = collections.libraryTagCollections[tag] %}
  {%- for item in libraryList -%}
      <li><a href="{{ item.url | url }}">{{ item.data.title }}</a></li>
  {%- endfor -%}
</ul>

<p>Back to <a href="{{ '/library/search/' | url }}">search</a>, to the <a href="{{ '/library/' | url }}">most recent</a> or <a href="{{ '/library/all' | url }}">full list</a> of photobooks</p>
