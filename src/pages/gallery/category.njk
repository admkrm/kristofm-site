---
layout: 'search'
class : 'gallery'
pagination:
  data: collections.galleryCategories
  size: 1
  alias: category
permalink: /{{class}}/search/categories/{{ category }}/
title: Gallery in category {{ category }}
---

<h1>Gallery for category "{{ category }}"</h1>

<ul>
  {% set galleryList = collections.galleryCategoryCollections[category] %}
  {%- for item in galleryList -%}
      <li><a href="{{ item.url | url }}">{{ item.data.title }}</a></li>
  {%- endfor -%}
</ul>

<p>Back to <a href="{{ '/gallery/search/' | url }}">search</a>, to the <a href="{{ '/gallery/' | url }}">most recent</a> or <a href="{{ '/gallery/all' | url }}">full list</a> of gallery items</p>
